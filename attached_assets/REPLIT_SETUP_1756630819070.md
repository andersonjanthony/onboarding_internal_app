# 🚀 Replit Setup Guide

## Step 1: Import to Replit

### Option A: Upload Folder
1. Go to [Replit.com](https://replit.com)
2. Click "Create Repl"
3. Choose "Upload folder" 
4. Drag and drop the entire `secureforce-onboarding` folder
5. Replit will auto-detect it as a React project

### Option B: GitHub Import (if you've pushed to GitHub)
1. Create new Repl → "Import from GitHub"
2. Enter your repository URL
3. Select "React (Vite)" template

## Step 2: Configuration

### Automatic Setup
Replit should automatically:
- Detect `package.json` and install dependencies
- Set up the run command (`pnpm dev`)
- Configure the preview window

### Manual Steps (if needed)
1. **Install Dependencies**:
   ```bash
   pnpm install
   ```

2. **Set Run Command** (in `.replit` file):
   ```
   run = "pnpm dev"
   ```

3. **Environment Variables** (optional):
   - Go to "Secrets" tab in Replit
   - Add `VITE_SLACK_WEBHOOK` and `VITE_N8N_WEBHOOK` if needed

## Step 3: Run the App

1. Click the "Run" button in Replit
2. Wait for the build to complete
3. Open the preview window
4. You should see the SecureForce onboarding app!

## 🎯 What You'll See

- **Header**: SecureForce branding with welcome message
- **Stepper**: 4-step progress indicator
- **Wizard Cards**: 3 main onboarding steps
- **System Info**: Client and environment summary
- **Calendar**: January 2025 with project milestones
- **Resources**: Security playbooks, tools, and training

## 🔧 Customization in Replit

### Quick Changes
- **Company Name**: Search for "SecureForce" in `App.tsx`
- **Client Data**: Edit `sampleClient` object
- **Milestones**: Modify `januaryMilestones` array
- **Colors**: Update Tailwind classes

### Testing Integrations
1. Get a Slack webhook URL from your workspace
2. Add it to Replit Secrets as `VITE_SLACK_WEBHOOK`
3. Test by clicking "Sign Agreement" - you should get a Slack notification

## 📱 Mobile Testing

Use Replit's mobile preview to test responsive design:
1. Click the preview window
2. Use browser dev tools or Replit's mobile view
3. Test all breakpoints (mobile, tablet, desktop)

## 🚀 Deployment from Replit

### Static Site Deployment
1. Build the project: `pnpm build`
2. The `dist` folder contains your production build
3. Deploy options:
   - **Replit Hosting**: Click "Deploy" → "Static Site"
   - **Download**: Download `dist` folder for other hosts

### Environment Variables for Production
Add these to your deployment platform:
- `VITE_SLACK_WEBHOOK` (optional)
- `VITE_N8N_WEBHOOK` (optional)

## 🛠 Troubleshooting

### Common Issues

**"Module not found" errors**:
```bash
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

**Build errors**:
```bash
pnpm build --verbose
```

**Port conflicts**:
- Replit automatically handles ports
- If needed, Vite uses port 5173 by default

### Getting Help
- Check the browser console for errors
- Use Replit's built-in debugger
- Review the `README.md` for detailed documentation

## ✅ Success Checklist

- [ ] App runs without errors
- [ ] All 3 wizard steps work
- [ ] Calendar displays milestones
- [ ] Responsive design works on mobile
- [ ] Optional: Slack/n8n integrations configured
- [ ] Build completes successfully (`pnpm build`)

---

**You're all set! 🎉 Your SecureForce onboarding app is ready for customization and deployment.**
